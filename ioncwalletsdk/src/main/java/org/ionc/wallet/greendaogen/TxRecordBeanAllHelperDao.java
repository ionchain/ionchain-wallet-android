package org.ionc.wallet.greendaogen;

import android.database.Cursor;
import android.database.sqlite.SQLiteStatement;

import org.greenrobot.greendao.AbstractDao;
import org.greenrobot.greendao.Property;
import org.greenrobot.greendao.internal.DaoConfig;
import org.greenrobot.greendao.database.Database;
import org.greenrobot.greendao.database.DatabaseStatement;

import org.ionc.wallet.bean.TxRecordBeanAllHelper;

// THIS CODE IS GENERATED BY greenDAO, DO NOT EDIT.
/** 
 * DAO for table "TX_RECORD_BEAN_ALL_HELPER".
*/
public class TxRecordBeanAllHelperDao extends AbstractDao<TxRecordBeanAllHelper, Long> {

    public static final String TABLENAME = "TX_RECORD_BEAN_ALL_HELPER";

    /**
     * Properties of entity TxRecordBeanAllHelper.<br/>
     * Can be used for QueryBuilder and for referencing column names.
     */
    public static class Properties {
        public final static Property Id = new Property(0, Long.class, "id", true, "_id");
        public final static Property IndexMaxForAll = new Property(1, Long.class, "indexMaxForAll", false, "INDEX_MAX_FOR_ALL");
        public final static Property PublicKey = new Property(2, String.class, "publicKey", false, "PUBLIC_KEY");
    }


    public TxRecordBeanAllHelperDao(DaoConfig config) {
        super(config);
    }
    
    public TxRecordBeanAllHelperDao(DaoConfig config, DaoSession daoSession) {
        super(config, daoSession);
    }

    /** Creates the underlying database table. */
    public static void createTable(Database db, boolean ifNotExists) {
        String constraint = ifNotExists? "IF NOT EXISTS ": "";
        db.execSQL("CREATE TABLE " + constraint + "\"TX_RECORD_BEAN_ALL_HELPER\" (" + //
                "\"_id\" INTEGER PRIMARY KEY AUTOINCREMENT ," + // 0: id
                "\"INDEX_MAX_FOR_ALL\" INTEGER," + // 1: indexMaxForAll
                "\"PUBLIC_KEY\" TEXT);"); // 2: publicKey
    }

    /** Drops the underlying database table. */
    public static void dropTable(Database db, boolean ifExists) {
        String sql = "DROP TABLE " + (ifExists ? "IF EXISTS " : "") + "\"TX_RECORD_BEAN_ALL_HELPER\"";
        db.execSQL(sql);
    }

    @Override
    protected final void bindValues(DatabaseStatement stmt, TxRecordBeanAllHelper entity) {
        stmt.clearBindings();
 
        Long id = entity.getId();
        if (id != null) {
            stmt.bindLong(1, id);
        }
 
        Long indexMaxForAll = entity.getIndexMaxForAll();
        if (indexMaxForAll != null) {
            stmt.bindLong(2, indexMaxForAll);
        }
 
        String publicKey = entity.getPublicKey();
        if (publicKey != null) {
            stmt.bindString(3, publicKey);
        }
    }

    @Override
    protected final void bindValues(SQLiteStatement stmt, TxRecordBeanAllHelper entity) {
        stmt.clearBindings();
 
        Long id = entity.getId();
        if (id != null) {
            stmt.bindLong(1, id);
        }
 
        Long indexMaxForAll = entity.getIndexMaxForAll();
        if (indexMaxForAll != null) {
            stmt.bindLong(2, indexMaxForAll);
        }
 
        String publicKey = entity.getPublicKey();
        if (publicKey != null) {
            stmt.bindString(3, publicKey);
        }
    }

    @Override
    public Long readKey(Cursor cursor, int offset) {
        return cursor.isNull(offset + 0) ? null : cursor.getLong(offset + 0);
    }    

    @Override
    public TxRecordBeanAllHelper readEntity(Cursor cursor, int offset) {
        TxRecordBeanAllHelper entity = new TxRecordBeanAllHelper( //
            cursor.isNull(offset + 0) ? null : cursor.getLong(offset + 0), // id
            cursor.isNull(offset + 1) ? null : cursor.getLong(offset + 1), // indexMaxForAll
            cursor.isNull(offset + 2) ? null : cursor.getString(offset + 2) // publicKey
        );
        return entity;
    }
     
    @Override
    public void readEntity(Cursor cursor, TxRecordBeanAllHelper entity, int offset) {
        entity.setId(cursor.isNull(offset + 0) ? null : cursor.getLong(offset + 0));
        entity.setIndexMaxForAll(cursor.isNull(offset + 1) ? null : cursor.getLong(offset + 1));
        entity.setPublicKey(cursor.isNull(offset + 2) ? null : cursor.getString(offset + 2));
     }
    
    @Override
    protected final Long updateKeyAfterInsert(TxRecordBeanAllHelper entity, long rowId) {
        entity.setId(rowId);
        return rowId;
    }
    
    @Override
    public Long getKey(TxRecordBeanAllHelper entity) {
        if(entity != null) {
            return entity.getId();
        } else {
            return null;
        }
    }

    @Override
    public boolean hasKey(TxRecordBeanAllHelper entity) {
        return entity.getId() != null;
    }

    @Override
    protected final boolean isEntityUpdateable() {
        return true;
    }
    
}
